<form method="post" action="{{ path('app_quiz_theme_delete', {'id': quiz_theme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quiz_theme.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
