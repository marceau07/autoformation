{% extends 'base.html.twig' %}

{% block title %}QuizShare{% endblock %}

{% block body %}
    <h1>QuizShare</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quiz_share.id }}</td>
            </tr>
            <tr>
                <th>Start_date</th>
                <td>{{ quiz_share.startDate ? quiz_share.startDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Finish_date</th>
                <td>{{ quiz_share.finishDate ? quiz_share.finishDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_quiz_share_index') }}">back to list</a>

    <a href="{{ path('app_quiz_share_edit', {'id': quiz_share.id}) }}">edit</a>

    {{ include('quiz_share/_delete_form.html.twig') }}
{% endblock %}
