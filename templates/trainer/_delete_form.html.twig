<form method="post" action="{{ path('app_trainer_delete', {'uuid': trainer.uuid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trainer.id) }}">
    <button class="btn btn-danger">{{ 'global.btn_delete'|trans }}</button>
</form>
