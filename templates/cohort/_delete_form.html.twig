<form method="post" action="{{ path('app_cohort_delete', { uuid: cohort.uuid }) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
  <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cohort.id) }}" />
  <button class="btn">Delete</button>
</form>
