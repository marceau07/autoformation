{% extends 'base.html.twig' %}

{% block title %}
  New Trainee
{% endblock %}

{% block main %}
  <div class="container-fluid">
    <div class="row d-flex flex-start">
      <div class="col-3 mt-2 mb-2 text-start">
        <a href="{{ path('app_trainee_index') }}" class="btn btn-success">{{ 'global.btn_back'|trans }}</a>
      </div>
      <div class="col-6 text-center mt-2 mb-2">
        <h1>{{ 'administration.trainee.create'|trans }}</h1>
      </div>
    </div>

    <div class="row">
      <div class="col-12">{{ include('trainee/_form.html.twig') }}</div>
    </div>
  </div>
{% endblock %}
