{% extends 'base.html.twig' %}

{% block title %}
  Edit Trainee
{% endblock %}

{% block main %}
  <div class="container-fluid">
    <div class="row flex-start">
      <div class="col-3 mt-2 mb-2 text-start">
        <a href="{{ path('app_trainee_index') }}" class="btn btn-success">{{ 'global.btn_back'|trans }}</a>
      </div>
      <div class="col-6 text-center mt-2 mb-2">
        <h1>Edit Trainee</h1>
      </div>
      <div class="col-3 mt-2 mb-2 text-end">
        {{ include('trainee/_delete_form.html.twig') }}
      </div>
    </div>

    <div class="row">
      <div class="col-12">
        {{ include('trainee/_form.html.twig', { button_label: 'global.btn_save'|trans }) }}
      </div>
    </div>
  </div>
{% endblock %}
