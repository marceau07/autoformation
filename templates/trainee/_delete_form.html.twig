<form method="post" action="{{ path('app_trainee_delete', { uuid: trainee.uuid }) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
  <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trainee.id) }}" />
  <button class="btn btn-danger">Delete</button>
</form>
